# Core Captive Portal & Proxy Dependencies
mitmproxy>=10.0.0
dnslib>=0.9.23
requests>=2.31.0
qrcode[pil]>=7.4.2

# ML & AI Classification Engine
scikit-learn>=1.3.2
numpy>=1.24.3
pandas>=2.0.3
joblib>=1.3.2

# Windows Network Management (Handled via PowerShell Bridge)
# winsdk>=1.0.0 (Removed due to build issues on Python 3.14)
# pywin32>=306 (Fallback to PS if fails)

# For web scraping (if needed)
# beautifulsoup4==4.12.2